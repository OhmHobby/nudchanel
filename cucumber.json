{
  "default": {
    "paths": ["features/**/*.feature"],
    "require": ["features/step_definitions/**/*.ts"],
    "requireModule": ["ts-node/register", "tsconfig-paths/register"],
    "format": ["@cucumber/pretty-formatter", "json:results.json"]
  }
}
