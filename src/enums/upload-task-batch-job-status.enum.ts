export enum UploadTaskBatchJobStatus {
  ready = 'ready',
  in_queue = 'in_queue',
  running = 'running',
  aborted = 'aborted',
  succeeded = 'succeeded',
  partial_success = 'partial_success',
  failed = 'failed',
}
